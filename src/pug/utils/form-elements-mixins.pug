mixin textField(options)
  -
    var textFieldName = 'name'
    var textFieldPleceHolder = 'placeholder'
    var textFieldTitle = 'title'
    var textFieldClass = 'class'
    var test = 'test'
  .input-row
    .input-320-wrapper
      h3
        label(for= options.name)= options.title
        if test
          span(class='ui-kit-right-text')= options.test
      input(type='text', name= options.name, placeholder= options.placeholder class='text-field', class= options.class)

mixin dropDownGuests(options)
  -
    var dropDownFieldTitle = 'title'
    var test = 'test'
  .input-row
    .input-320-wrapper
      h3
        label(for= options.name)= options.title
        if test
          span(class='ui-kit-right-text')= options.test  
      .iqdropdown
        p.iqdropdown-selection(data-seletion-text='Сколько гостей')
        .iqdropdown-menu
          .iqdropdown-menu-option(data-id='item1')
            div
              p.iqdropdown-item Взрослые
          .iqdropdown-menu-option(data-id='item2')
            div
              p.iqdropdown-item Дети
          .iqdropdown-menu-option(data-id='item3')
            div
              p.iqdropdown-item Младенцы
          .iqdropdown-menu-last-row
            p.iqdropdown-menu-clear-btn Очистить
            p.iqdropdown-menu-submit-btn Применить

mixin dropDownRooms(options)
  -
    var dropDownFieldTitle = 'title'
    var test = 'test'
    var open = 'open'
  .input-row
    .input-320-wrapper
      h3
        label(for= options.name)= options.title
        if test
          span(class='ui-kit-right-text')= options.test      
      .iqdropdown(class= options.open)
        p.iqdropdown-selection(data-seletion-text='2 спальни, 2 кровати...', data-text-plural='2 спальни, 2 кровати...')
        .iqdropdown-menu
          .iqdropdown-menu-option(data-id='item11', data-defaultcount="2")
            div
              p.iqdropdown-item Спальни
          .iqdropdown-menu-option(data-id='item22', data-defaultcount="2")
            div
              p.iqdropdown-item Кровати
          .iqdropdown-menu-option(data-id='item33')
            div
              p.iqdropdown-item Ванные комнаты


mixin dateDropdown150(options)
  -
    var textFieldName = 'name'
    var textFieldPleceHolder = 'placeholder'
    var textFieldTitle = 'title'  
  .input-150-wrapper
    h3
      label(for= options.name)= options.title
      if test
        span(class='ui-kit-right-text')= options.test
    .text-field-150-wrapper(class='arrow-down')
      input(type='text', name= options.name, placeholder= options.placeholder class='text-field text-field-150', class= 'datepicker-here')


mixin filterDateDropdown(options)
  -
    var textFieldName = 'name'
    var textFieldPleceHolder = 'placeholder'
    var textFieldTitle = 'title'
  .input-row
    .input-266-wrapper
      h3
        label(for= options.name)= options.title
        if test
          span(class='ui-kit-right-text')= options.test
      .text-field-266-wrapper(class='arrow-down')
        input(type='text', name= options.name, placeholder= options.placeholder class='text-field text-field-266', class= 'datepicker-here' data-range='true' data-multiple-dates-separator=' - ' data-date-format='dd M')


mixin subscriptionTextField(options)
  -
    var textFieldName = 'name'
    var textFieldPleceHolder = 'placeholder'
    var textFieldTitle = 'title'
  .input-row
    .input-266-wrapper
      h3
        label(for= options.name)= options.title
        if test
          span(class='ui-kit-right-text')= options.test
      .text-field-266-wrapper(class='arrow-forward')
        input(type='text', name= options.name, placeholder= options.placeholder class='text-field text-field-266')


mixin checkboxButton(options)
  -
    var checkboxName = 'name'
    var checkboxClass = 'class'
    var checkboxTitle = 'title'
    var checkboxId = 'id'
  .input-row
    .input-320-wrapper      
      input(type='checkbox', name= options.name, id= options.id, class='checkbox-default')
      label(for= options.id)= options.title


mixin radioButton(options)
  -
    var radioName = 'name'
    var radioClass = 'class'
    var radioTitle = 'title'
    var radioId = 'id' 
  div.radio-wrapper
    input(type='radio', name= options.name, id= options.id, class='radio-default')
    label(for= options.id)= options.title


mixin toggleSwitch(options)
  -
    var toggleName = 'name'
    var toggleClass = 'class'
    var toggleTitle = 'title'
    var toggleId = 'id'
    var toggleChecked = 'checked' 
  .input-row.toggle-wrapper
    label(for= options.id, class='switch')      
      input(type='checkbox', name= options.name, id= options.id, class=options.class)
      span(class='slider round')
    span(class='toggle-text')= options.title


mixin likeButton(options)
  -
    var likeName = 'name'
    var likeClass = 'class'
    var likeTitle = 'title'
    var likeId = 'id'
    var likeChecked = 'checked'
    var likeCount = 'count'   
  label(for= options.id, class='switch-like')      
    input(type='checkbox', name= options.name, id= options.id, class=options.class)
    span(class='slider-like round')
      span(class='like-count')= options.count


